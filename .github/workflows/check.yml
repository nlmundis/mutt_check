name: check

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: ["3.9", "3.10", "3.11", "3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      # Installs the conditional tomli dependency on 3.10 and earlier.
      - run: pip install .
      # ruff, mypy and tomli for mypy, each pinned so a new release cannot turn this build red on its own.
      - run: pip install -r requirements-dev.txt
      - run: make check PYTHON=python
